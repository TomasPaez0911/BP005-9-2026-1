#include <stdio.h>

int main() {
    int i = 1; // variable de control 

    do {
        printf("%d\n" , i); // muestra el valor actual 
        i++; // actualiza la variable 
        } while (i <= 5); //la condicion se verifica al final 

        return 0;
}
