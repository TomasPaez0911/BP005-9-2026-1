#include <stdio.h>

int main() {
    int i; // variable de control
    
    for(i = 1; i <= 5; i++) { //inicio, condicion y actualizacion
    printf("%d\n" ,  i); //muestra el valor actual
} 
return 0;
}
